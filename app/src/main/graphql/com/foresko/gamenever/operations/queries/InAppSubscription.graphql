query InAppSubscriptionQuery($id: ID!) {
    node(id: $id) {
        ... on InAppSubscription {
            id
            price {
                currencyCode
                value
            }
            sku
        }
    }
}